// Single source of truth for the app's version.
//
// Info.plist references these, and Scripts/release.sh reads them to derive the
// tag and the Sparkle appcast entry. Bump here and nowhere else.

MARKETING_VERSION = 1.0.4
CURRENT_PROJECT_VERSION = 5
