# Copas

A clipboard manager for macOS.

Download the latest release from
[GitHub](https://github.com/sigitkusuma/copas/releases/latest).

This page exists to host `appcast.xml`, the update feed every copy of Copas
checks. The feed URL is compiled into every build, so it can never move.
